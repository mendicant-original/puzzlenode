- title "#{@user.nickname}'s Profile"

#profile
  %h1
    = gravatar_image @user, 48
    = link_to @user.nickname, github_url(@user)

  #stats
    %h2 Stats

    %h3 Problems Solved: #{@user.submissions.correct.count}
    - if @leaderboard
      %h3
        Leaderboard position: #{@leaderboard}
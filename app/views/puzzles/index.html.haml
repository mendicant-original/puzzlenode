#puzzles
  - @puzzles.each do |puzzle|
    .puzzle
      = link_to puzzle.name, puzzle_path(puzzle)
      
      .description
        .action
          %button.clean-gray{:'data-url' => puzzle_path(puzzle)}
            View Puzzle
        = puzzle.short_description
        .clearfix
#puzzles
  - @puzzles.each do |puzzle|
    //= link_to puzzle_path(puzzle) do
    .puzzle
      .name
        = link_to "#{puzzle.name} &rarr;".html_safe, puzzle_path(puzzle)
        
          
      .description= puzzle.short_description
#puzzles
  - @puzzles.each do |puzzle|
    //= link_to puzzle_path(puzzle) do
    .puzzle
      %h3= link_to puzzle.name, puzzle_path(puzzle)
      
      .description
        .action
          %button.cupid-green{:'data-url' => new_puzzle_submission_path(puzzle)} Submit my solution
        = puzzle.short_description
        .clearfix